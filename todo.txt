In order:

Revamp portfolio section - so it includes a screenshot and the title underneath, then when you hover over it, it slides down a bit to the description and github link

Find out a way to get profile pic to hide particles.


API rate limiting - saves information and reloads only at night - prolly works if i host on server

(optional - dark mode light mode switch somwehere idk if its useful prolly not, prolly yes to flex knowledge)

yep connect to internet ig - after various code polishing.
